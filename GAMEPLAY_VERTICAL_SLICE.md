# GAMEPLAY VERTICAL SLICE — 森循島《西子灣·柴山》第一版可玩流程

> 角色視角：遊戲製作人 + 主程式
> 對象場景：`taiwan3d/south.html`
> 目的：把現在的「可走動場景」變成一條**有頭有尾、5–10 分鐘可玩完**的垂直切片。
> 驗收依據：`PLAYTEST_SPEC.md` 的準則 1–10。
> 最後更新：2026-06-19

---

## 1. 設計主軸

**一句話**：玩家是「森循島環境調查員」，從西子灣海灘出發，沿柴山步道上山，沿途**調查地貌、解鎖在地知識卡、完成環境修復**，最後登上觀景台俯瞰高雄港結算。

- 主題與既有大遊戲（垃圾回收 `DESIGN.md`）一致：**環境/生態守護**，但切片先聚焦「探索＋在地知識＋輕量修復」，不引入戰鬥與垃圾怪（屬後續）。
- 教育內容取材自《南部地圖：西子灣·鼓山·柴山研究與設計》PDF：柴山＝**隆起珊瑚礁石灰岩**、**台灣獼猴**（約 1,000–1,400 隻、勿餵食的「恐懼地景」概念）、**石灰岩溶洞/泰國谷**、**白榕森林**、西子灣夕照/打狗英國領事館/旗津沙洲。

### 玩家動詞（這版只做這些）
1. **移動**（WASD/方向鍵）— 已有。
2. **接近互動**（走近互動點 → 顯示提示 → 按 `E`）— 需新增。
3. **調查/收集/修復**（按 `E` 的三種結果，視互動點類型）— 需新增。
4. **閱讀知識卡**（解鎖後彈出可閱讀，可在卡冊回看）— 需新增。

> 範圍控制：**不做**戰鬥、垃圾怪、背包經濟、夥伴、分類小遊戲、存檔。鏡頭沿用固定 diorama 跟隨。

---

## 2. 流程總覽（狀態機）

```
[開始畫面] ──按 開始──▶ [操作教學(浮層)] ──走幾步/按E──▶
  目標1 調查西子灣海岸 ──▶ 目標2 沿柴山步道上山 ──▶
  目標3 找到石灰岩洞穴/泰國谷 ──▶ 目標4 完成環境修復 ──▶
[任務完成畫面(結算+知識卡冊)]
```

- 任何時刻 HUD 顯示：**目前地點**（常駐）＋**當前目標**（可勾選、隨進度更新）＋已解鎖知識卡數。
- 目標完成時：打勾動畫＋提示音/文字「目標完成！」＋（若有）解鎖知識卡彈窗。
- 用**目標指引**（畫面邊緣箭頭或地面光柱指向下個目標座標）解決「玩家不知道往哪走」的問題（見 §6 發現）。

座標皆來自 `heightmap_south.js` 既有 `S.markers`（地圖 380×480，出生點 `spawn≈(150,294)`）。

---

## 3. 開始畫面

- 全螢幕半透明浮層：遊戲標題「**森循島 — 西子灣·柴山**」、一句引導文（「沿著夕陽下的海岸，走上這座由珊瑚礁長成的山。」）、**［開始探索］** 按鈕（或「按任意鍵／點擊開始」）。
- 背景：直接用已渲染的場景（夕照海岸）當底，營造氛圍；按開始後浮層淡出。
- 驗收對應：準則 4（進場取景）、10（明確起點）。

## 4. 操作教學（進場後浮層/提示條）

- 三張小提示依序或同畫面：`WASD／方向鍵 移動`、`走近發光點 → 按 E 調查`、`目標顯示在左上，跟著畫面邊緣的箭頭走`。
- 玩家移動或完成第一次互動後自動關閉。
- 驗收對應：準則 2、7 的可學習性。

---

## 5. 四個目標（含真實座標、互動、知識卡、回饋）

### 目標 1 — 調查西子灣海岸
- **地點**：西子灣海灘 `(140,286)`～出生點 `(150,294)`（生態帶 `sand`/`reef`）。
- **玩家要做**：沿沙灘與礁岩走，找到 **3 個發光調查點**（海漂垃圾/礁石/潮間帶生物）→ 逐一按 `E` 調查/拾取。
- **互動點**：3 個 `inspect` 點散佈在海灘與礁岩海岸（`inland < 44`）。第 1 個放在出生點正前方數步，當教學示範。
- **知識卡 KC1｜隆起珊瑚礁海岸．西子灣**：柴山是海底珊瑚礁被板塊抬升、出露海面而成的石灰岩山；西子灣以夕陽、中山大學、1879 年打狗英國領事館與旗津沙洲聞名。
- **完成回饋**：3/3 後「目標完成！」＋解鎖 KC1 ＋目標 2 指引亮起（指向 `龍泉寺登山口`）。
- **現有資產**：沙灘/礁岩生態帶、`reefStack` 離岸礁石（`south_props.js`）。
- **需新增**：3 個 inspect 互動點與發光標記。

### 目標 2 — 沿柴山步道上山
- **地點**：`龍泉寺登山口 (178,252)` → 沿 `biome==="trail"` 的步道往東北爬升（`trailX(d)` 蜿蜒路徑）。
- **玩家要做**：走到登山口（指引帶路）→ 按 `E` 進入步道 → 沿步道往上，途中遇到 **獼猴互動點**（`E` 觀察，提示「請勿餵食」）。
- **互動點**：登山口 `trailhead`（觸發）＋步道中段 1 個 `monkey` 觀察點。
- **知識卡 KC2｜台灣獼猴與「恐懼地景」**：柴山約 1,000–1,400 隻台灣獼猴；餵食會改變其行為、增加人猴衝突；維持「人不可預測」的恐懼地景，獼猴才會與人保持距離 → **不餵食、收好食物**。
- **完成回饋**：抵達步道高處檢查點 → 「目標完成！」＋解鎖 KC2 ＋指引指向洞穴/泰國谷。
- **現有資產**：`trail` 生態帶（已壓平、淨空、土黃配色）、`regions.at` 會顯示「柴山登山步道」。
- **需新增**：步道沿線檢查點；**注意**：見 §6，現有地標未必落在 `trailX` 路徑上，需把互動點放在實際可走的步道帶上或調整地標座標。

### 目標 3 — 找到石灰岩洞穴或泰國谷
- **地點**（擇一或兩者皆設指引，先到先觸發）：`天雨洞 (300,250)` / `猴洞 (82,232)` / `泰國谷 (300,110)`。
- **玩家要做**：循指引抵達洞口（`south_props.js` 已有 `caveEntrance` 暗色洞口）或泰國谷峽谷 → 按 `E` 調查。
- **互動點**：洞口 `cave` 調查點（落在既有 cave marker 上）。
- **知識卡 KC3｜石灰岩溶洞與泰國谷**：雨水含弱酸、長期溶蝕石灰岩，形成猴洞、天雨洞等溶洞與峽谷地形（泰國谷因林相鬱閉如熱帶而得名）；洞中可見鐘乳石、滴石。
- **完成回饋**：「目標完成！」＋解鎖 KC3 ＋指引指向觀景台（環境修復點）。
- **現有資產**：`caveEntrance`/`cliff` 道具、`CANYON` 峽谷地形。
- **需新增**：洞口/峽谷 inspect 互動點。

### 目標 4 — 完成一個環境修復互動
- **地點**：`柴山觀景台 (332,44)` 附近（山頂帶，俯瞰高雄港）的**復育點**。
- **玩家要做**：到復育點按 `E` →「清除外來種銀合歡苗／種回原生白榕」小互動（按 `E` 數次或一次完成，附簡單進度條）。
- **互動點**：`restore` 修復點 1 個。
- **知識卡 KC4｜柴山森林修復**：原生**白榕**（纏勒榕）是石灰岩森林指標；外來種**銀合歡**會排擠原生植物；玩家的修復行動＝清除入侵種、復育原生樹，呼應「不餵食、垃圾不落地、步道減量」。
- **完成回饋**：修復完成動畫 → 直接進入**任務完成畫面**。
- **現有資產**：`cairn` 山頂石堆（觀景台地標）、`banyan` 白榕散佈、山頂裸岩。
- **需新增**：restore 互動點與簡單進度互動。

---

## 6. 設計必須處理的既有問題（來自 2026-06-19 QA 測試）

> 這些是 Tier 1 自動測試與程式碼審查發現的「會影響可玩性」的點，做切片時必須一併處理。

1. **地標與可走步道不重合**：`trailX(d)` 步道是程式生成的蜿蜒路徑，但 `龍泉寺登山口(178,252)` 等地標**不一定**落在步道帶上（實測偏離數十單位）。→ 解法：互動點放在「實際 `biome==="trail"` 的格子」上，或微調地標座標貼齊步道。
2. **沒有方向指引，玩家會迷路**：實測從出生點 `(150,294)` 一路按 W（向北）只會走到地圖北緣的森林帶，**到不了山頂/觀景台**（山頂在東北 `x≈330`）。→ 必做**目標指引**（邊緣箭頭或地面光柱指向下個目標座標）。
3. **移動本身沒問題**：實測八方向順暢、海岸碰撞穩、不掉海、不出界（`maxStep=3.0` 在測試走廊未造成卡死）。但**完整「海灘→觀景台」含轉向路線**仍須 Tier 2 瀏覽器實走確認沒有礁岩/陡坡死角。
4. **地點銘牌會淡出**：需改為 HUD **常駐**顯示目前地點（準則 1）。

---

## 7. 需新增的系統（主程式視角）

全部以**新增檔案 + 既有檔案最小增修**完成，沿用 `SENXUN.*` 命名空間與 `main.js` 既有的 build/update 掛鉤模式（如同 `south_props.js` 的 `if (SENXUN.props)` 掛法）。

| 系統 | 建議檔案 | 職責 |
|------|---------|------|
| 互動/接近偵測 | `js/interactions_south.js`（`SENXUN.interactions`） | 定義互動點清單（座標/類型/綁定知識卡）；每幀算最近點、顯示「按 E」提示；處理 `E` 觸發。 |
| 任務狀態機 | `js/quest_south.js`（`SENXUN.quest`） | 4 目標的進度、完成判定、推進、`done` 旗標；對外提供 `quest()` 狀態。 |
| 知識卡 | `js/cards_south.js`（`SENXUN.cards`） | 卡片資料（KC1–KC4 文字）、解鎖、卡冊 UI。 |
| 介面/浮層 | 擴充 `js/ui_south.js` + `south.html` | 開始畫面、教學浮層、常駐地點+目標 HUD、目標指引箭頭、完成畫面、卡片彈窗。 |
| 輸入 | 擴充 `js/main.js` | 監聽 `KeyE`/開始鍵；把 `keys` 傳給 interactions/quest 更新。 |
| 測試掛鉤 | 任一處掛 `SENXUN.debug` | 供 Tier 2 Playwright 讀狀態（見 `PLAYTEST_SPEC.md` §5.5）。 |

**不破壞既有**：新檔只給 `south.html` 載入；`main.js` 用 `if (SENXUN.xxx)` 守衛呼叫，全台版 `taiwan.html` 不受影響（與現有南部檔的做法一致）。

---

## 8. 對 PLAYTEST_SPEC 準則的對應

| 準則 | 由切片的哪一塊滿足 |
|------|------------------|
| 1 玩家知道在哪 | HUD 常駐地點名（改 `ui_south.js`） |
| 2 知道目標 | HUD 當前目標 + 隨進度更新（`quest_south.js`） |
| 3 移動順暢 | 沿用既有 player（已驗）＋ Tier 2 全路線複驗 |
| 4 鏡頭舒服 | 開始畫面取景 + 沿用固定跟隨鏡頭 |
| 5 看得出地貌 | 既有生態帶/道具 + 目標帶玩家經過全部地貌 |
| 6 任務起點 | 開始畫面 + 目標1 第一個互動點 |
| 7 互動點 | `interactions_south.js`（≥6 個互動點）|
| 8 知識卡 | KC1–KC4（`cards_south.js`）|
| 9 完成回饋 | 每目標打勾+音效/文字、卡片彈窗 |
| 10 玩 5–10 分鐘 | 4 目標串成的完整路線 |

---

## 9. 實作順序與進度

**增量 1（2026-06-19 完成、全綠）— 互動骨幹 + 目標 1 + 知識卡**
- [x] 測試掛鉤 `SENXUN.debug` + HUD 常駐地點/目標/進度（`main.js`、`ui_south.js`）。
- [x] 互動系統:接近提示 +「E」觸發（`interactions_south.js`）。
- [x] 任務狀態機:4 目標（`quest_south.js`;目標 1 完整,2–4 佔位）。
- [x] 知識卡資料 + 彈窗 UI（`cards_south.js`;KC1 於目標 1 完成時解鎖）。
- [x] 目標完成 toast；順手修 F7（HUD 對比）。
- 驗證:`slice_logic.cjs` 20/20、Tier 2 #7/#9 綠;見 `AUTONOMOUS_DEV_LOG.md` #3。

**增量 2（2026-06-19 完成、全綠）— 走完整條路線**
- [x] 目標 2 步道/獼猴互動點（互動點貼齊真實 `trail` 格 → 解 F2）。
- [x] 目標 3 天雨洞（石灰岩溶洞）互動點。
- [x] 目標 4 山頂觀景台環境修復互動。
- [x] 目標指引 🎯 pin（投影到螢幕，解 F1 迷路）。
- [x] 開始畫面 + 完成（結算）畫面 → Tier 2 #8 轉綠（9/9 無 skip）。
- [x] 降低鏡頭俯角/距離（F6，52°→36°）。
- [x] 全路線可達性驗證（route_probe：beach→trail→cave→summit 全到得了）。

**後續可做（非阻塞）**
- [ ] 把「玩 5-10 分鐘」做穩（目前完整流程約 3-6 分鐘）：加支線互動點、收集物、或更多知識卡。
- [ ] 鏡頭手感依實玩再微調（F6 已降，仍可調）。
- [ ] 真人試玩驗收（DoD 人工項）。

> 每完成一步，跑 Tier 1（迴歸）+ Tier 2，並把結果寫入 `AUTONOMOUS_DEV_LOG.md`。
