# PLAYTEST_SPEC — 森循島《西子灣·柴山》可玩性規格

> 角色視角：遊戲製作人 + 主程式 + QA 遊玩測試員
> 對象場景：`taiwan3d/south.html`（南部正式場景，全台版 `taiwan.html` 不在此規格內）
> 最後更新：2026-06-19

本文件回答一個問題：**「這個專案在什麼條件下，才算『真的可以玩』？」**
並定義一套**可重複執行的自動化遊玩測試**來客觀驗收，而不是靠感覺。

---

## 0. 目前版本快照（2026-06-19 誠實評估）

現況是一個**「可走動的場景模型 / tech demo」**，還不是一款遊戲。

實際做得到：
- 用 WASD／方向鍵操控一隻角色，地形跟隨、不會掉海、有坡度上限。
- 沿途地點銘牌會依位置淡入（西子灣海灘／礁岩海岸／柴山步道／山頂…）。
- 夕照光下能看到海→沙灘→礁岩→石灰岩森林→山頂的縱向分層，含洞口、峭壁、山頂石堆、離岸礁石等道具。
- 固定 diorama 跟隨鏡頭（穩定、無開發者滑桿與數值）。

實際**做不到**（這就是「還不能玩」的原因）：
- 沒有開始畫面、沒有真正的操作教學（只有一行 `WASD／方向鍵 移動探索` 字）。
- HUD 的「目標」是一行**寫死的旁白文字**，不會更新、無法追蹤、不會完成。
- 場景裡所有東西（洞穴、峭壁、礁石、地標）都是**純佈景**，不能互動、不能調查、不能撿、不能修復。
- 沒有任何知識卡、沒有任務、沒有完成回饋、沒有結算畫面 → 玩家不知道「要幹嘛」「何時算贏」。

> 結論：要從 demo 變成「可玩的垂直切片」，缺的是**遊戲系統層**（任務狀態機、互動/接近偵測、知識卡 UI、開始/結束畫面、目標指引），而不是地形。詳見 `GAMEPLAY_VERTICAL_SLICE.md`。

> **進度（2026-06-19）**：已完成**增量 1+2**——完整可玩切片：開始畫面 → 海岸調查 → 步道上山(獼猴) → 石灰岩溶洞 → 山頂環境修復 → 完成結算，4 張在地知識卡，目標指引 pin，鏡頭已降俯角。**準則 1-9 達成、10 接近(完整流程約 3-6 分鐘)**；F1/F2/F3/F6/F7 皆已處理。自動測試 Tier 1（13/13+32/32）+ Tier 2（9/9 無 skip）全綠。逐筆進度見 `AUTONOMOUS_DEV_LOG.md`。

---

## 1. 「可玩」定義（驗收清單）

每一條都給出：**定義 → 可驗收標準（客觀） → 目前狀態**。
狀態圖示：✅ 已達成　⚠️ 部分達成　❌ 未達成

| # | 準則 | 可驗收標準（怎麼算過） | 目前狀態 |
|---|------|----------------------|---------|
| 1 | **玩家知道自己在哪裡** | 任何時刻畫面上都能看到目前地點名稱；進入新區域會有提示。 | ✅ HUD 常駐顯示目前地點 + 進入新區大標淡入（增量1）。 |
| 2 | **玩家知道現在目標是什麼** | HUD 持續顯示「當前目標」文字，且該目標**會隨進度更新**並能被標記為完成。 | ✅ HUD 目標+進度，隨互動更新、目標可完成（增量1）。 |
| 3 | **移動順暢，不卡住、不掉海、不穿地形** | 八方向皆可動；碰海岸被擋下（不進深海）；不穿過地形；不被陡坡瞬移；長時間移動仍在地圖內。 | ✅ Tier 1 13/13 + Tier 2 移動/碰撞測試通過（§4）。完整含轉向「海灘→山頂」全路線待目標 2-4。 |
| 4 | **鏡頭舒服，不像開發者工具** | 無滑桿/座標/海拔等開發者讀數；鏡頭平滑跟隨、不抖動、不穿地形；進場有合理取景。 | ✅ 已降俯角(52°→36°)+拉近、開始畫面提供進場取景；可再依實玩微調(F6)。 |
| 5 | **場景能看出海岸/沙灘/礁岩/森林/步道/洞穴/山頂** | 七種地貌在畫面上肉眼可辨，且位置符合「離海越遠越高」。 | ✅ 生態帶+道具齊備、分層已驗（海≈1.8→山頂≈70）、Tier 2 截圖肉眼確認。 |
| 6 | **至少一個任務起點** | 場景中有明確的任務觸發點（接近/互動即開始第一個任務）。 | ✅ 目標 1 自開場啟動、首個互動點即起點（增量1）；開始畫面待增量2。 |
| 7 | **至少一個互動點** | 至少一個物件可被玩家觸發（接近顯示提示 → 按鍵互動 → 有結果）。 | ✅ 海岸 3 個互動點可觸發（接近提示→E→進度），Tier 2 #7 驗證（增量1）。 |
| 8 | **至少一張知識卡** | 互動後可解鎖一張在地知識卡（柴山地質/獼猴/西子灣…），有 UI 可閱讀。 | ✅ KC1 可解鎖+彈窗閱讀，Tier 2 #9 驗證（增量1）；KC2-4 待目標 2-4。 |
| 9 | **至少有任務完成回饋** | 完成目標時有明確回饋（音效/動畫/文字/打勾），玩家確知「我做到了」。 | ✅ 進度更新 + 「目標完成」toast + 知識卡彈窗（增量1）。 |
| 10 | **至少能玩 5–10 分鐘** | 一條有起點到結算的流程，正常遊玩 5–10 分鐘可走完。 | ⚠️ 完整流程已成形(開始→4目標→完成),實走約 3-6 分鐘;要穩定 5-10 分鐘可再加支線/收集(後續)。 |

### 1.1 垂直切片「可玩」門檻（Definition of Done）

當以下**全部**成立，視為達到第一版「可玩」：

- [ ] 準則 1–10 全部 ✅。
- [ ] 自動化測試 Tier 1 **與** Tier 2 全綠（見 §4）。
- [ ] 一次真人試玩可在 5–10 分鐘內：看懂操作 → 完成 4 個目標 → 解鎖 ≥3 張知識卡 → 看到完成畫面，全程無 console error、無卡死、無掉海。
- [ ] 測試結果已寫入 `AUTONOMOUS_DEV_LOG.md`。

---

## 2. 測試環境

- 已安裝：**Node v21.6.2**、**npm 10.4.0**、**Python 3.12.2**。
- 本機靜態伺服器（擇一）：`cd taiwan3d && python -m http.server 8080` → `http://localhost:8080/south.html`。
- three.js：目前由 `https://cdnjs.cloudflare.com/.../r128/three.min.js` 載入（**網路相依**；見 §5 建議在地化以利離線/CI）。

---

## 3. 自動化遊玩測試方案（總覽）

採**兩層**設計，兼顧「快速、零安裝的邏輯防線」與「真實瀏覽器的整體驗收」：

| 層 | 名稱 | 工具 | 安裝需求 | 涵蓋準則 | 狀態 |
|----|------|------|---------|---------|------|
| **Tier 1** | 邏輯冒煙測試 | Node + 極小 THREE stub | **零安裝**（已有 Node） | 3、5(高度)、1/2 的資料面 | ✅ 已建立並通過 |
| **Tier 2** | 瀏覽器遊玩測試 | Playwright + Chromium | **需安裝（待你確認）** | 1–10 全部，特別是 console/互動/任務/截圖 | 🔶 待核准後建立 |

### 使用者列出的 8 項檢查 → 由哪一層負責

| 使用者要求的檢查 | Tier 1（今天可跑） | Tier 2（需 Playwright） |
|------------------|:--:|:--:|
| 用瀏覽器打開 south.html | — | ✅ |
| 檢查 console error | 語法層 `node --check` ✅ | 執行期 `pageerror`/`console` ✅ |
| 模擬 WASD/方向鍵移動 | 邏輯模擬 ✅ | 真實鍵盤事件 ✅ |
| 玩家座標有改變 | ✅ | ✅ |
| 玩家不會掉進海裡 | ✅ | ✅ |
| UI 顯示目前地點與任務 | 地名解析 ✅ | DOM 銘牌+任務 HUD 可見 ✅ |
| 至少觸發一個互動點 | （待切片）| ✅（切片完成後）|
| 任務可以完成 | （待切片）| ✅（切片完成後）|
| 每次結果寫入 DEV LOG | ✅ | ✅ |

> 注意：「觸發互動點 / 任務完成」這兩項在**遊戲系統做出來之前無法驗**（目前根本沒有互動與任務）。因此先以 `GAMEPLAY_VERTICAL_SLICE.md` 把功能做出來，Tier 2 才有東西可測。

---

## 4. Tier 1 — 邏輯冒煙測試（已建立，零安裝）

- 檔案：`taiwan3d/tests/logic_smoke.cjs`
- 執行：`node taiwan3d/tests/logic_smoke.cjs`（結束碼 0=通過）
- 原理：用 Node `vm` 依 `south.html` 順序載入邏輯層（util/config/south_scene/heightmap_south/regions_south/player），以極小 THREE stub 驅動 `player.build/update`，斷言底層保證。**不開瀏覽器、不裝套件、不改動遊戲檔。**

涵蓋（硬性斷言）：
1. `SENXUN_ISLAND`/`spawn` 正確；出生點在陸地（非海）、`groundY` 有效、地名可解析。
2. 按 W/A/S/D：座標有改變、方向合理、**全程未掉進海裡**、留在地圖內。
3. 按 S 朝海：海岸碰撞有效（停在岸邊不入海）。
4. 縱向分層：山頂明顯高於海灘。

外加觀察項（非硬性）：可攀爬性探針、各地標可達性與地名解析、沿內陸高度取樣。

**2026-06-19 基線結果：13 硬性檢查全部 PASS；12 支 JS `node --check` 全通過。** 完整輸出見 `AUTONOMOUS_DEV_LOG.md`。

---

## 5. Tier 2 — 瀏覽器遊玩測試（提案，需你核准後安裝）

> 依你的指示：**不先自行安裝**。以下說明原因與要裝的套件，請你確認。

### 5.1 為什麼需要 Playwright（而非純 Node/jsdom）
本遊戲是 **Three.js WebGL canvas**。要做到你列的「用瀏覽器打開、抓 console error、模擬真實鍵盤、確認畫面有東西、截圖」，需要**真正的瀏覽器與 GL context**——jsdom/happy-dom 沒有 WebGL，無法渲染也無法跑遊戲迴圈。Playwright 能：
- 啟動 Chromium、載入 `south.html`、等待遊戲迴圈起跑。
- 監聽 `pageerror` 與 `console` → 滿足「檢查 console error」。
- 發送**真實** `keydown/keyup`（產生正確的 `e.code`，如 `KeyW`）→ 滿足「模擬 WASD」。
- `page.evaluate(() => window.SENXUN.debug...)` 讀遊戲狀態 → 驗座標改變/未掉海/任務狀態。
- 截圖存證 → 驗「場景看得出地貌」「鏡頭舒服」「UI 有顯示」。

### 5.2 要安裝的套件（**待你點頭**）
| 套件 | 用途 | 大小/備註 |
|------|------|----------|
| `@playwright/test`（dev dependency，`npm i -D`） | 測試執行器 + 瀏覽器自動化 | npm 套件，數 MB |
| `npx playwright install chromium` | 下載 Chromium 瀏覽器二進位 | 約 120–170 MB 下載 |

> 靜態伺服器**不需額外套件**：用既有的 `python -m http.server`（或 Playwright 內建 `webServer` 設定）即可。
> 會建立的檔案（不改既有遊戲檔）：`taiwan3d/package.json`、`taiwan3d/playwright.config.mjs`、`taiwan3d/tests/playtest.spec.mjs`。

### 5.3 一個需要你決定的選項：three.js 在地化
目前 three.js 走 cdnjs（**網路相依**）。建議把 `three.min.js`(r128) 下載到 `taiwan3d/vendor/` 並讓測試用的頁面指向它，使測試**離線/CI 可重現**、不受 CDN 影響。是否要這麼做請一併告知（預設：建議做）。

### 5.4 Tier 2 測試案例（規劃；切片完成後實作）
1. **載入無誤**：開頁 → 等 `window.SENXUN` 就緒 → `pageerror` 數 = 0、無紅色 `#err`。
2. **開始畫面**：標題/開始鍵可見 → 互動後進入遊戲。
3. **移動**：壓住 `KeyW` 1 秒 → `SENXUN.debug.tile()` 改變、`inland` 增加、`terr>1`（未掉海）。
4. **海岸碰撞**：壓住 `KeyS` 3 秒 → 全程 `terr>1`。
5. **地點/任務 UI**：DOM 中地點銘牌與任務 HUD 文字非空且可見。
6. **互動點**：走到第一個互動點 → 提示出現 → 按 `KeyE` → `SENXUN.debug.quest()` 進度 +1。
7. **知識卡**：互動解鎖 → 卡片 UI 顯示 → `SENXUN.debug.cards().length ≥ 1`。
8. **任務完成**：依序完成 4 目標 → 出現完成畫面 → `SENXUN.debug.quest().done === true`。
9. **截圖**：每個目標完成後存一張 `tests/shots/*.png` 作視覺存證。

### 5.5 需要在切片中加入的測試掛鉤（debug hooks）
為了讓 Tier 2 穩定（不靠脆弱的 DOM 爬取），請在做切片時於 `window` 暴露唯讀除錯介面，例如：
```js
SENXUN.debug = {
  tile()   { return SENXUN.player.tile(); },        // {x,y}
  region() { /* 目前地點名稱 */ },
  quest()  { /* {current, index, total, done} */ },
  interactablesInRange() { /* [{id,name,dist}] */ },
  cards()  { /* 已解鎖知識卡 id 陣列 */ }
};
```

---

## 6. 測試結果如何記錄

每次跑 Tier 1 / Tier 2，都**追加**一筆到 `AUTONOMOUS_DEV_LOG.md`，至少包含：
- 日期時間、版本（git commit）、執行的層級與指令。
- 結果摘要（PASS/FAIL 數、結束碼）、截圖路徑（Tier 2）。
- 新發現的問題（findings）與後續行動。

---

## 7. 範圍界線

- 本規格只涵蓋 `south.html`。全台版 `taiwan.html` 與 `index.html`（垃圾回收主遊戲）不在此次驗收範圍。
- 「可玩」門檻指**第一版垂直切片**，不要求完整遊戲內容（戰鬥、垃圾怪、夥伴系統等屬後續，見 `DESIGN.md`）。
